declare module '@mui/material/styles' {
	interface Theme {
		boxShadow: string;
		borderRadius: string;
	}
	
	interface ThemeOptions {
		boxShadow: string;
		borderRadius: string;
	}

	interface Palette {
		background: object;
	}

	interface PaletteOptions {
		background: object;
	}
}